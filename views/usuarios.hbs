<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{titulo}}</title>
    <style>
        /* Estilos para el fondo de pantalla con una imagen */
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: Arial, sans-serif;
            background-image: url('/images/ballena.jpg'); /* Ruta a tu imagen de fondo */
            background-size: cover; /* Asegura que la imagen cubra toda la pantalla */
            background-position: center; /* Centra la imagen */
            background-repeat: no-repeat; /* Evita que la imagen se repita */
        }
        
        /* Estilos para la tarjeta */
        .card {
            background-color: CadetBlue;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            max-width: 400px;
            width: 100%;
            text-align: center;
            color: black;
        }
        
        .card h1 {
            margin-bottom: 20px;
            font-size: 24px;
        }
        
        .card p {
            font-size: 16px;
            color: black;
            margin: 10px 0;
        }

        .card .nombre {
            font-weight: bold;
            font-size: 18px;
            margin-top: 15px;
            color: black;
        }
    </style>
</head>
<body>
    <div class="card">
        <h1>{{titulo}}</h1>
        <p><strong>Mensaje:</strong> {{mensaje}}</p>
        <p><strong>Verbo:</strong> {{verbo}}</p>
        <p class="nombre"><strong>Nombre:</strong> {{nombre}}</p>
    </div>
</body>
</html>

