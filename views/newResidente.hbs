<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Datos del Residente</title>

    <!-- Importar Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Estilos personalizados -->
    <style>
        /* Estilo para centrar el card */
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #f0f0f0;
        }

        /* Estilo para el card */
        .card {
            width: 100%;
            max-width: 500px;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            background-color: white;
        }

        .card h3 {
            text-align: center;
            margin-bottom: 20px;
        }

        /* Estilo para los atributos */
        .attribute {
            margin-bottom: 10px;
        }
    </style>
</head>
<body>

    <div class="card">
        <h3>Datos del Residente</h3>

        <!-- Mostrar los datos del residente en la tarjeta -->
        <div class="attribute">
            <strong>Cédula:</strong> {{datosResidente.cedula}}
        </div>
        <div class="attribute">
            <strong>Nombre:</strong> {{datosResidente.nombre}}
        </div>
        <div class="attribute">
            <strong>Apellidos:</strong> {{datosResidente.apellidos}}
        </div>
        <div class="attribute">
            <strong>Email:</strong> {{datosResidente.email}}
        </div>
        <div class="attribute">
            <strong>Password:</strong> {{datosResidente.password}} <!-- Normalmente se debe encriptar -->
        </div>
        <div class="attribute">
            <strong>Casa:</strong> {{datosResidente.casa}}
        </div>
        <div class="attribute">
            <strong>Celular:</strong> {{datosResidente.celular}}
        </div>
        <div class="attribute">
            <strong>Categoría:</strong> {{datosResidente.categoria}}
        </div>
        <div class="attribute">
            <strong>Rol:</strong> {{datosResidente.rol}}
        </div>
    </div>

    <!-- Incluir Bootstrap 5 JS desde el CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
