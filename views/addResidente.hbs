<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Añadir Residente</title>

    <!-- Importar Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Estilos personalizados -->
    <style>
        /* Estilo para el cuerpo de la página */
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
        }

        /* Estilo para el card */
        .card {
            width: 100%;
            max-width: 600px; /* Limita el tamaño del card */
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            background-color: white;
        }

        .card h3 {
            text-align: center;
            margin-bottom: 20px;
        }

        /* Estilo para organizar los campos horizontalmente dentro del fieldset */
        .horizontal-group {
            display: flex;
            gap: 15px;
            justify-content: space-between;
        }

        .horizontal-group .form-group {
            flex: 1;
        }

        /* Botón de enviar */
        .btn-submit {
            width: 100%;
        }

        fieldset {
            margin-bottom: 20px;
            border: 1px solid #ddd;
            padding: 10px;
        }

        legend {
            font-size: 1.2rem;
            margin-bottom: 10px;
            width: auto;
        }
    </style>
</head>
<body>

    <div class="card">
        <h3>Añadir Residente</h3>

        <!-- Formulario para añadir residente -->
        <form action="/addResidente" method="POST">

            <!-- Agrupar Cedula, Nombre y Apellidos en un fieldset -->
            <fieldset>
                <legend>Información Personal</legend>
                <div class="horizontal-group">
                    <!-- Cédula -->
                    <div class="form-group">
                        <label for="cedula" class="form-label">Cédula</label>
                        <input type="text" class="form-control" id="cedula" name="id_cedula" maxlength="10" required>
                    </div>

                    <!-- Nombre -->
                    <div class="form-group">
                        <label for="nombre" class="form-label">Nombre</label>
                        <input type="text" class="form-control" id="nombre" name="nombre" required>
                    </div>

                    <!-- Apellidos -->
                    <div class="form-group">
                        <label for="apellidos" class="form-label">Apellidos</label>
                        <input type="text" class="form-control" id="apellidos" name="apellidos" required>
                    </div>
                </div>
            </fieldset>

            <!-- Agrupar Email, Password y Celular en un fieldset -->
            <fieldset>
                <legend>Credenciales</legend>
                <div class="horizontal-group">
                    <!-- Email -->
                    <div class="form-group">
                        <label for="email" class="form-label">Email</label>
                        <input type="email" class="form-control" id="email" name="email" required>
                    </div>

                    <!-- Password -->
                    <div class="form-group">
                        <label for="password" class="form-label">Password</label>
                        <input type="password" class="form-control" id="password" name="password" required>
                    </div>

                    <!-- Número de celular -->
                    <div class="form-group">
                        <label for="celular" class="form-label">Celular</label>
                        <input type="text" class="form-control" id="celular" name="celular" maxlength="15" required>
                    </div>
                </div>
            </fieldset>

            <!-- Agrupar Casa, Categoría y Rol en un fieldset -->
            <fieldset>
                <legend>Detalles Residenciales</legend>
                <div class="horizontal-group">
                    <!-- Casa -->
                    <div class="form-group">
                        <label for="casa" class="form-label">Casa</label>
                        <input type="text" class="form-control" id="casa" name="casa" maxlength="20" required>
                    </div>

                    <!-- Categoría (Propietario o Inquilino) -->
                    <div class="form-group">
                        <label for="categoria" class="form-label">Categoría</label>
                        <select class="form-select" id="categoria" name="categoria" required>
                            <option value="Propietario">Propietario</option>
                            <option value="Inquilino">Inquilino</option>
                        </select>
                    </div>

                    <!-- Rol (Master o Slave) -->
                    <div class="form-group">
                        <label for="rol" class="form-label">Rol</label>
                        <select class="form-select" id="rol" name="rol" required>
                            <option value="Master">Master</option>
                            <option value="Junior">Junior</option>
                        </select>
                    </div>
                </div>
            </fieldset>

            <!-- Botón para enviar el formulario -->
            <button type="submit" class="btn btn-primary btn-submit">Añadir Residente</button>
        </form>
    </div>

    <!-- Incluir Bootstrap 5 JS desde el CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
